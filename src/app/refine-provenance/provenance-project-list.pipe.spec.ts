import { ProvenanceProjectListPipe } from './provenance-project-list.pipe';

describe('ProvenanceProjectListPipe', () => {
  it('create an instance', () => {
    const pipe = new ProvenanceProjectListPipe();
    expect(pipe).toBeTruthy();
  });
});
