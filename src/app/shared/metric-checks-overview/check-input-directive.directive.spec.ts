import { CheckInputDirectiveDirective } from './check-input-directive.directive';

describe('CheckInputDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckInputDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
